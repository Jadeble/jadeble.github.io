---
title: "CHAPTER 1"
date: 2026-05-04
weight: 3
toc: true
---

# 进程概念

让一个程序假设它独占一台机器：进程向程序提供“看上去”私有的，其他程序无法访读写的内存系统（地址空间），以及一颗看上去只执行该程序的CPU。

xv6使用页表为程序提供独有的地址空间，页表使用硬件实现，不同架构有不同的实现接口

xv6为每个进程维护不同的页表，每一块地址空间包含了从虚拟地址0开始的用户内存，从下往上依次是`进程指令`、`全局变量`、`栈区`、用户可按需拓展的`堆区`（malloc）。

